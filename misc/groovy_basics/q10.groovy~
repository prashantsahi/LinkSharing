//10. Create a file which contains all the odd numbered lines of a given file. Each line should be numbered at the beginning of line viz : 1, 3, 5.....


File f = new File("odd output")
int i = 1
f.text = ''
new File("./abc").eachLine {
  if(i%2!=0)
   {
   f << i + it
     }
  f<< "\n"
  i++
}
println f.text
